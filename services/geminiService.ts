// AI features have been removed from this application.
// These stubs ensure no runtime errors occur if imports were missed during refactoring.

export const generateWebhookConfig = async (prompt: string): Promise<any[]> => {
  console.warn("AI features are disabled.");
  return [];
};

export const analyzeWebhookError = async (errorMsg: string, webhookUrl: string): Promise<string> => {
  console.warn("AI features are disabled.");
  return "AI analysis unavailable.";
}